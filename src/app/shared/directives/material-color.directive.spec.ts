import { MaterialColorDirective } from './material-color.directive';

describe('MaterialColorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialColorDirective();
    expect(directive).toBeTruthy();
  });
});
